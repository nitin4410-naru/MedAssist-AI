version: "3.9"

services:
  medassist-ai:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8501:8501"
    extra_hosts:
      - "host.docker.internal:host-gateway"
